<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gjl.music.playback.mapper.PlayCountMapper">

    <!-- ══════════════════════════════════════════════════
         MySQL: 单条 SQL 完成 upsert + 30s 去重
         ══════════════════════════════════════════════════ -->

    <insert id="incPlayCount" databaseId="mysql">
        INSERT INTO play_count (user_id, item_id, item_type, play_count, last_played_at)
        VALUES (#{userId}, #{itemId}, #{itemType}, 1, NOW())
        ON DUPLICATE KEY UPDATE
            play_count = IF(TIMESTAMPDIFF(SECOND, last_played_at, NOW()) >= 30,
                            play_count + 1, play_count),
            last_played_at = IF(TIMESTAMPDIFF(SECOND, last_played_at, NOW()) >= 30,
                                NOW(), last_played_at)
    </insert>

    <!-- H2: 单条 SQL upsert，去重全靠 Caffeine 层（30s TTL） -->
    <insert id="incPlayCount" databaseId="h2">
        INSERT INTO play_count (user_id, item_id, item_type, play_count, last_played_at)
        VALUES (#{userId}, #{itemId}, #{itemType}, 1, CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE
            play_count = play_count + 1,
            last_played_at = CURRENT_TIMESTAMP
    </insert>

    <!-- ══════════════════════════════════════════════════
         查询
         ══════════════════════════════════════════════════ -->

    <select id="batchGetSongCounts" resultType="java.util.HashMap">
        SELECT CAST(item_id AS SIGNED) AS itemId, play_count
        FROM play_count
        WHERE user_id = #{userId}
          AND item_type = 'song'
          AND item_id IN
          <foreach collection="songIds" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <select id="getPlayCount" resultType="java.lang.Integer">
        SELECT play_count FROM play_count
        WHERE user_id = #{userId} AND item_id = #{itemId} AND item_type = #{itemType}
    </select>

    <!-- ══════════════════════════════════════════════════
         级联清理
         ══════════════════════════════════════════════════ -->

    <delete id="deleteByItem">
        DELETE FROM play_count WHERE item_id = #{itemId} AND item_type = #{itemType}
    </delete>

    <!-- ══════════════════════════════════════════════════
         艺术家播放量聚合：song → artist，全局/按用户
         ══════════════════════════════════════════════════ -->
    <select id="getTopArtistIds" resultType="java.util.HashMap">
        SELECT sa.artist_id AS "artistId", SUM(pc.play_count) AS "totalPlays"
        FROM play_count pc
        INNER JOIN song_artist sa ON pc.item_id = sa.song_id
        WHERE pc.item_type = 'song'
        <if test="userId != null">AND pc.user_id = #{userId}</if>
        GROUP BY sa.artist_id
        ORDER BY SUM(pc.play_count) DESC
        LIMIT #{limit}
    </select>

</mapper>
